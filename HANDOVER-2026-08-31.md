# HANDOVER — WorkerKit + Moltwork
**Date:** 2026-08-31 06:30 UTC
**Session:** Integration sprint — runtime-letta fixed, E2E proven, mwgym started

---

## Current State

### Runtime
- **runtime-letta**: UP on :3000 (PID 3195635)
- **Model**: opencode-go/mimo-v2.5 (reasoning model)
- **Config**: stateless:true, maxSteps:2, permissionMode:unrestricted
- **Latency**: ~35s per run (5 reasoning passes × ~4s each + 15s tool)
- **File creation**: VERIFIED (MOLTWOK_SMOKE_OK, x402 ideas)

### Test Results
- 91/91 workerkit invariants PASS
- 17/17 Letta adapter tests PASS
- 63/63 qdw unit tests PASS
- E2E: opportunity → campaign → run → grade → regrade → graph PASS

### Campaigns
- LIVE-001: real Letta wrote 3 x402 ideas, graded v0=1.0, regraded v1=0.0 v2=0.3
- O001-O010: 10 synthetic campaigns, brief compounding working
- Graph: 39 nodes, 12 edges in hydradb.db

### What's Wired (tested, working)
1. FakeWorkerRuntime — ms-speed orchestration tests
2. MockHarbor — Docker-free trials + regrade
3. RunBinding — content-addressed statements
4. GraphStore — SQLite with auto-projection on campaign save
5. Regrade — v0/v1/v2 with calibration
6. LabBrief — compounds evidence across campaigns
7. AutomationResolver — API→MCP→WebMCP→human ladder
8. MarketPacks — Roblox/Upwork/Etsy/Hackathon
9. Opportunity schema — execution_steps, human_deps
10. Campaign lifecycle — create/run/grade/regrade/outcome
11. mw_labkit — FakeWorkerRuntime, MockHarbor, RunBinding, LettaClient
12. ontology.py — shared types (ExecutionStep, HumanDependency, CredentialRef)

### What's NOT Wired
1. Harbor CLI — MockHarbor only, `harbor run` not called
2. Trace2Skill — vendor/ cloned, never called
3. GEPA — vendor/ cloned, never called
4. HydraDB over Bolt — SQLite only, HTTP client ready
5. Real submissions — all synthetic
6. Letta reasoning — can't disable (model feature, not bug)

### Known Issues
- Letta stream never terminates (check file as success condition)
- reasoningEffort can't be set on stateless sessions
- mimo-v2.5 always reasons before acting (5+ API calls per turn)
- Campaign filesystem mismatch fixed (use Campaign.dir() as cwd)

### HydraDB
- SQLite: data/hydradb.db
- Nodes: 39 (campaigns, runs, bindings)
- Edges: 12 (campaign→run→binding)
- lab_runs: 1 row
- HTTP client ready for Bolt

### Git Status
- workerkit: pushed to ethonline-2026
- oracle: pushed to master
- lab-campaigns: 22 campaigns committed
- lab-worlds: technical-submission-v0 committed

## Immediate Next Steps
1. Wire Harbor CLI for real trials (not MockHarbor)
2. Run 10 real Letta campaigns (35s each = 6min total)
3. Test Trace2Skill on Letta trajectories
4. Wire HydraDB over Bolt when available
5. Build mwgym project

## Key Files
```
runtime-letta: services/runtime-letta/src/index.ts
campaign: campaign.py
regrade: regrade.py
lab_brief: lab_brief.py
graph: hydra/graph.py, hydra/client.py
ontology: ontology.py
mw_labkit: mw_labkit/{runtime,harbor,records,hashing}.py
packs: market_packs.py
automation: automation.py
```

## Architecture (frozen)
```
ORACLE → opportunity with execution_steps
    ↓
MOLTWORK → campaign + brief from prior evidence
    ↓
┌───────────┬───────────┐
│ LETTA     │ HARBOR    │
│ persistent│ world +   │
│ worker    │ execution │
└───────────┴───────────┘
    ↓
WorkerRun = Harbor Trial + WorkerVersion + Campaign + Receipt
    ↓
HYDRADB → derived evidence graph
    ↓
CGE → "what should we try next?"
```
